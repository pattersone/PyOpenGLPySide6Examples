If you have not already, setup your Python venv, then:
pip install pyside6 numpy
pip install PyOpenGL
(or possibly pip install PyOpenGL PyOpenGL_accelerate)

...
Can use previous venv and just:
pip install PyOpenGL

